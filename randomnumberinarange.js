function randomRange(myMin, myMax) {
  // Only change code below this line
  return Math.floor(Math.random() * (myMax- myMin+1))+myMin; //This returns a random whole integer between the range.
  // Only change code above this line
}
